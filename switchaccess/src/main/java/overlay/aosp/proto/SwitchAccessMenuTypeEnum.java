package com.google.android.accessibility.switchaccess.proto;

/** Defines an enum of possible types of a menu. */
public class SwitchAccessMenuTypeEnum {

  /** Possible types of a menu. */
  public enum MenuType {
    TYPE_UNDEFINED,
    TYPE_GLOBAL,
    TYPE_ACTION
  }
}
